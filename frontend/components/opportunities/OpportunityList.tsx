import EmptyState from "@/components/ui/EmptyState";

export default function OpportunityList() {
  return (
    <EmptyState
      title="Oportunidades listas para scoring"
      description="Este módulo queda preparado para cruzar estado, pagos, overlays y reglas de negocio."
    />
  );
}
